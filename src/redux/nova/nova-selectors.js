export const selectNovaState = ({ nova }) => nova;
export const selectWarehouses = ({ nova }) => nova.warehouses;
export const selectCities = ({ nova }) => nova.cities;
export const selectCitiesLoading = ({ nova }) => nova.citiesLoading;
export const selectWarehousesLoading = ({ nova }) => nova.warehousesLoading;
